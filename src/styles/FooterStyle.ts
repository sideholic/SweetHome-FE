import styled from "styled-components";

/* Wrapper */
export const FooterWrapper = styled.footer`
  //border-top: 1px solid ${(props) => props.theme.greenColor.green3};
  background-color: ${(props) => props.theme.greenColor.green3};
  height: 30vh;
`;
